## 张量基础操作

1、创建未初始化的张量

```python
torch.empty()
```

torch可理解为一个张量库 是一个科学计数库

| 科学计数    | npmpy   | pytorch |
|:--------|:--------|:--------|
| numpy   | ndarray |         |
| pytorch |         | torch   |

初始化有基础数据空间的张量`torch.empty(5, 3)`声明一个五行三列的初始化张量。

创建大小张量`torch.rand(5, 3)`，这是个左闭右开的数组

在vscode中执行torch相关指令
```angular2html
1、创建未初始化的张量
2、创建随机初始化张量
3、创建normalnize distrbution正太分布的张量
4、创建全零的张量
5、指定张量数据类型的张量
6、从指定数据中创建张量
7、创建全1 的张量
8、创建一个形状一样的张量，随机张量、全1/0张量
9、创建一个形状一样，内容全为指定值的张量
10、创建多维数组，确定当前张量对象类型，确定张量中元素类型为什么
```

**张量运算**







